{
  "private": true,
  "name": "@sentio-example/pyth-staking",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "sentio build --skip-deps",
    "build:all": "pnpm --filter=$(node -p \"require('./package.json').name\")... build",
    "gen": "sentio gen",
    "test": "sentio test",
    "upload": "sentio upload"
  },
  "dependencies": {
    "@sentio/sdk": "workspace:*"
  },
  "devDependencies": {
    "@sentio/cli": "workspace:*"
  }
}
