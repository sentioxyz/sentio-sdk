{
  "name": "@sentio/protos",
  "license": "Apache-2.0",
  "version": "1.0.0",
  "scripts": {
    "compile": "tsc -p .",
    "clean": "tsc --build --clean",
    "build": "yarn compile"
  },
  "dependencies": {
    "google-protobuf": "^3.15.8",
    "nice-grpc": "^2.0.0"
  },
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "module": "./lib/index.js",
  "files": [
    "{lib,src}",
    "!{lib,src}/tests",
    "!**/*.test.{js,ts}"
  ]
}
